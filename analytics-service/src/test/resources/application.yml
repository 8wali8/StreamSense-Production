spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

streamsense:
  logos:
    # No MinIO in a test: logo bytes live in a map for the life of the context.
    store: memory
