# CVE ids to suppress, one per line, each with a reason and the follow-up that removes it.

# torch 2.2.2+cpu and transformers 4.40.2 are pinned by ml-engine/pyproject.toml; moving to torch >= 2.6 and
# transformers >= 4.48 changes inference behaviour and needs its own verified branch (model outputs compared
# before and after). torch.load is only ever given the weights the image ships or downloads from the pinned
# Hugging Face revisions, never user input. Tracked in docs/planning/branches/14-supply-chain.md.
CVE-2025-32434
CVE-2024-11392
CVE-2024-11393
CVE-2024-11394
CVE-2026-4372
CVE-2026-5241
CVE-2026-9856
