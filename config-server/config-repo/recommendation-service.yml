# config-server/config-repo/reccomendation-service.yml

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka # url through docker

management:
  endpoints:
    web:
      exposure:
        include: health, info