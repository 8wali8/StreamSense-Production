# config-server/config-repo/api-gateway.yml

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka # url through docker

management:
  endpoints:
    web:
      exposure:
        include: health, info