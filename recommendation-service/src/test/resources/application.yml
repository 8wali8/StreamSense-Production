spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

streamsense:
  services:
    sentiment-service:
      base-url: http://localhost:8083
    video-service:
      base-url: http://localhost:8084
